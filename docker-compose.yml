version: '3.8'

services:
  mileage-app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:80"
    restart: unless-stopped
    networks:
      - mileage-network

networks:
  mileage-network:
    driver: bridge
  mileage-network:
    driver: bridge
