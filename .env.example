# ===========================================
# Environment Configuration
# ===========================================

# Email Service (Resend)
RESEND_API_KEY=re_DspNS5do_8B3kBa1PJoapT9SXcr57rXJm

# Server Configuration
PORT=3001


# Admin / From addresses (used by the email server)
# ADMIN_EMAIL is the recipient for administrative copies (e.g., jesus@irishtaxagents.com)
ADMIN_EMAIL=jesus@irishtaxagents.com

# FROM_NAME and FROM_ADDRESS control the "From" header shown to recipients
# Example: FROM_NAME="Nexus Ventures" and FROM_ADDRESS="log@happydreamsireland.com"
FROM_NAME="Nexus Ventures"
FROM_ADDRESS=log@happydreamsireland.com

# Application Configuration
NODE_ENV=production

# PocketBase (client-side) - used at build time by the frontend
# Example: VITE_POCKETBASE_URL=https://pocketbase.your-domain.com
VITE_POCKETBASE_URL=http://127.0.0.1:8090

# Google reCAPTCHA (v3) keys - optional but recommended for forms like password reset
# RECAPTCHA_SECRET is used by the server to verify the token
# VITE_RECAPTCHA_SITE_KEY is used by the client to obtain a token
RECAPTCHA_SECRET=your_recaptcha_secret_key_here
VITE_RECAPTCHA_SITE_KEY=your_recaptcha_site_key_here

# Security (Optional - for additional security)
# VITE_API_BASE_URL=https://your-api-domain.com
# VITE_ENABLE_ANALYTICS=false
# VITE_SENTRY_DSN=your_sentry_dsn_here

# Additional useful variables
# ADMIN_EMAIL: address that receives administrative copies of submitted PDFs (already above)
# FROM_NAME / FROM_ADDRESS: friendly from header used for outgoing emails (already above)